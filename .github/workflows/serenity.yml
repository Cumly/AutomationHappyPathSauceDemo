name: Serenity BDD Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Instalar Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Cache Maven
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Ejecutar pruebas
        # El error estaba en el '-' antes de run. Debe ir indentado debajo de name.
        run: mvn clean verify -Dwebdriver.driver=chrome -Dchrome.switches="--headless;--window-size=1920,1080;--no-sandbox;--disable-dev-shm-usage"

      - name: Guardar reporte Serenity
        if: always() # Esto asegura que el reporte se suba aunque los tests fallen
        uses: actions/upload-artifact@v4
        with:
          name: serenity-report
          path: target/site/serenity